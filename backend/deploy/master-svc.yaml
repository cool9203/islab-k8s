apiVersion: v1
kind: Service
metadata:
  name: islab-k8s-service
  namespace: kube-system
spec:
  selector:
    app: islab-k8s-master
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
